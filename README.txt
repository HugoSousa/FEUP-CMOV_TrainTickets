How to execute the project

Deploy the database on the same machine as the server (standard mysql)
Deploy the server (npm install + npm start)
Deploy the external debt service (npm install + npm start)

Change the IP on ApiRequest class on both android projects to the server IP.

Deploy the user application
Deploy the employee application


Client account:

Username: hugo
Password: hugo
--
Employee account:

Email: hugo@gmail.com
Password: hugo